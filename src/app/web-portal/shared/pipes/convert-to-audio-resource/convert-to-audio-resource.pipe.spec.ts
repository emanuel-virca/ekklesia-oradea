import { ConvertToAudioResourcePipe } from './convert-to-audio-resource.pipe';

describe('ConvertToAudioResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToAudioResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
